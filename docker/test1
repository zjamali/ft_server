#sudo apt update
#apt upgrade -y
	*install nginx and start it*
#apt install -y nginx
#service nginx start

	*install mysql server*
#apt install -y mariadb-server mariadb-client
#service mysql start

	*install PHP*
#apt install -y php-fpm php-mysql php-cli
#service php7.3-fpm start

	* edit the default.conf file *
#rm /etc/nginx/sites-enabled/default
#mv default.conf /etc/nginx/conf.d/default.conf 
#service nginx restart 
#service php7.3-fpm start

	* configure the mariadb sql *
#mysql -u root -p then press enter
	use mysql;

	update user set plugin='mysql_native_password' where user='root';

	flush privileges; 

	quit;


	* preparation to install myphpadmin extention for my phpadmin to connect with databse *
#apt install -y php-json php-mbstring
#apt install wget -y

	*install myphpadmin*
#wget https://files.phpmyadmin.net/phpMyAdmin/4.9.4/phpMyAdmin-4.9.4-all-languages.tar.gz

#tar -zxvf phpMyAdmin-4.9.4-all-languages.tar.gz
#mv phpMyAdmin-4.9.4-all-languages /usr/share/phpMyAdmin
---> move config.inc.php to /usr/share/phpMyAdmin/config.inc.php
	#mv config.inc.php /usr/share/phpMyAdmin/config.inc.php

---> create database
#mysql < /usr/share/phpMyAdmin/sql/create_tables.sql -u root -p then press enter
#mysql -u root -p
	then press enter 

	GRANT ALL PRIVILEGES ON phpmyadmin.* TO 'pma'@'localhost' IDENTIFIED BY 'pmapass';

	FLUSH PRIVILEGES;
quit;

----> create a config file for myphpadmin
#mv phpMyAdmin.conf /etc/nginx/conf.d/phpMyAdmin.conf
---> create temp folder fo myphpadmin
#mkdir /usr/share/phpMyAdmin/tmp
#chmod 777 /usr/share/phpMyAdmin/tmp
--->set the ownership of directory
#chown -R www-data:www-data /usr/share/phpMyAdmin

---> restart servecies
#service nginx restart
#service php7.3-fpm restart
#service mysql restart

